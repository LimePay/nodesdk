const BASE_SANDBOX = 'https://sandbox-api.limepay.io';
const BASE_PRODUCTION = 'https://api.limepay.io';

module.exports = {
    v1: {
        Sandbox: `${BASE_SANDBOX}/v1`,
        Production: `${BASE_PRODUCTION}/v1`
    },
    Sandbox: `${BASE_SANDBOX}/v1`,
    Production: `${BASE_PRODUCTION}/v1`
}